/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { TickerService } from "../ticker.service";
import { TickerCreateInput } from "./TickerCreateInput";
import { TickerWhereInput } from "./TickerWhereInput";
import { TickerWhereUniqueInput } from "./TickerWhereUniqueInput";
import { TickerFindManyArgs } from "./TickerFindManyArgs";
import { TickerUpdateInput } from "./TickerUpdateInput";
import { Ticker } from "./Ticker";

export class TickerControllerBase {
  constructor(protected readonly service: TickerService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Ticker })
  async create(@common.Body() data: TickerCreateInput): Promise<Ticker> {
    return await this.service.create({
      data: data,
      select: {
        id: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Ticker] })
  @ApiNestedQuery(TickerFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<Ticker[]> {
    const args = plainToClass(TickerFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Ticker })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: TickerWhereUniqueInput
  ): Promise<Ticker | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Ticker })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: TickerWhereUniqueInput,
    @common.Body() data: TickerUpdateInput
  ): Promise<Ticker | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Ticker })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: TickerWhereUniqueInput
  ): Promise<Ticker | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
